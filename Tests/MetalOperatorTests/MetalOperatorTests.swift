    import XCTest
    @testable import MetalOperator

    final class MetalOperatorTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(MetalOperator().text, "Hello, World!")
        }
    }
